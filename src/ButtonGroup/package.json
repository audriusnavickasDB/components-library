{
    "name": "@audnavlib/button-group",
    "description": "Button Group component",
    "version": "1.0.0",
    "license": "MIT",
    "author": "Audrius Navickas",
    "main": "dist/index.js",
    "publishConfig": {
        "access": "public"
    },
    "scripts": {
        "build": "cross-env webpack -p --config ../../webpack.config.js",
        "clean": "rimraf dist && rimraf node_modules && rimraf package-lock.json",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@audnavlib/button": "^1.0.0"
    },
    "peerDependencies": {
        "react": "^16.12.0",
        "react-dom": "^16.12.0"
    }
}
